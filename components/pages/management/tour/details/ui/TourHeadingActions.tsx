type TourHeadingActionsProps = {
  id: string;
  published: boolean;
};

const TourHeadingActions = ({ id, published }: TourHeadingActionsProps) => {
  return <div>TourHeadingActions</div>;
};

export default TourHeadingActions;
